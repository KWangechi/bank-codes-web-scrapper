/**
 * Handle pagination of the results on a page to reduce on render time
 * Later, will make this into a class
 * @param {Array} searchResults Array of search results
 */
export function paginateResults(searchResults) {
// define a pagination object that will have the following:
// rowsPerPage, totalRows, pageNumber, totalNumberOfPages


}